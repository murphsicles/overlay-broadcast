// Auto-converted from /tmp/overlay-broadcast/crates/cli/src/error.rs

use thiserror::Error;
/// Errors surfaced by the CLI.
#[derive(Debug, Error)]
pub enum CliError {
 BadInput(&str),
 Operation(&str),
 Selftest(usize),
 Reproduce(String),
}

