// Auto-converted from /tmp/overlay-broadcast/crates/cli/src/error.rs

/// Errors surfaced by the CLI.
pub enum CliError {
    BadInput(String),
    Operation(String),
    Selftest(usize),
    Reproduce(String),
}
