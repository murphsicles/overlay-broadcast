// Zeta entry point for overlay-broadcast.
// Transpiled from Rust and adapted for Zeta's stdlib.
use cli::{ run, Cli };

fn main() {
    let args = Cli::parse();
    match run(args) {
        Ok(output) => print!("{output}"),
        Err(error) => eprintln!("error: {error}"),
    }
}
